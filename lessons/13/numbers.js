const num = 1.5;

/*
console.log(Math.floor(num)); // 1
console.log(Math.ceil(num)); // 2
console.log(Math.round(num)); // 2
*/

/*
const randNum = Math.floor(Math.random() * 101);

if (Number.isNaN(randNum - 1)) {
  console.log('введите число');
} else {
  console.log('спасибо, ты молодец');
}
*/

/* 
const inputValue = '13  aafas 12';

console.log( Number(inputValue) * 2 );
console.log( Number.parseInt(inputValue) * 2 );
*/

/* 
console.log( Math.max(10, 15, 4, 11, 25) ); // 25
console.log( Math.min(10, 15, 4, 11, 25) ); // 4
*/

console.log( Math.pow(3, 2) );
console.log( Math.sqrt(9) );